Generated by `npm run generate:quickcalm-audio` (requires `ELEVENLABS_API_KEY`
in `.env.local`). Produces `<exercise-key>.mp3` + `<exercise-key>.json` per
Quick Calm exercise. Until these exist for a given exercise, Quick Calm
falls back to the Web Speech API player automatically — nothing breaks.
